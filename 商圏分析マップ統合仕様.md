# 商圏分析マップ (OSM / Leaflet版) 統合仕様

gis_drive_analysis.md で解析した「GIS DRIVE」の機能をベースに、Google Maps の代わりに
OpenStreetMap + Leaflet で構築してきた商圏分析ツールに、既存の「エリアプランニングシステム」
（Esri製・店舗単位の折込/配達エリア計画システム）が持つ機能を統合した仕様です。
似た機能は統合し、後者にしかない機能（店舗管理、配達プラン作成ウィザード、予算通数による
自動エリア選定、実データの統計指標など）を新たに追加しています。

boundaries/ フォルダの町丁目・字境界データ(全国、KEY_CODE付き)を使って、選択した商圏範囲に
含まれる実際の町丁目・字の境界線を描画する点は従来どおりです。

## 使い方

### 1. ローカルサーバーを起動する(必須)

boundaries/ は全国327MBあるため、選択範囲に応じて必要なファイルだけをその都度 fetch() で
読み込みます。ブラウザは file:// で開いたページからの fetch() をセキュリティ上ブロックする
ため、**ローカルサーバー経由で開く必要があります**(index.html をダブルクリックするだけでは
町丁目境界・検索・統計データ読み込みが動作しません)。

```bash
# このフォルダ(GISドライブ)で実行
python -m http.server 8000
```

ブラウザで http://localhost:8000/index.html を開いてください。

### 2. 基本操作

1. 左パネルで地点を検索するか、地図を直接操作して商圏を指定したいエリアまで移動。
2. ベースマップは地形図(OSM相当)・衛星写真・道路地図の3種類から切り替え可能。
3. 「商圏の指定方法」から形状を選択:
   - **円形**: 地図をクリックして中心を指定 → スライダー、または数値入力で半径(100〜5000m)を調整。
   - **多角形(任意商圏)**: 地図上をクリックして頂点を追加 → 始点付近をクリック、またはダブルクリックで確定。「一つ戻す」で直前の頂点を取り消し。
   - **所要時間(自動車・徒歩)**: 移動手段(徒歩/自転車/車)と分数を指定し、地図をクリックして起点を指定。
     - OpenRouteService の無料APIキーを入力すると、実際の道路網に基づく等時間圏(isochrone)を取得。
     - 未入力の場合は「速度 × 時間」による近似円で代替表示。
   - **電車商圏(新規)**: 起点(店舗)から最寄り駅・鉄道網を考慮した到達圏を指定するモード。APIキー等の外部連携が無い場合は、最寄り駅までの徒歩圏＋概算の鉄道到達範囲として簡易表示する。
   - **地域(市区町村・町丁目)**: 都道府県→市区町村を選択、または表示されている町丁目・字の境界をクリックして複数選択。地図を動かすと表示範囲の境界が自動で読み込まれる。
   - **多店舗分析(新規)**: 「店舗管理」に登録済みの複数店舗をまとめて選択し、各店舗ごとの商圏(円形/所要時間など)を一括作成して比較・集計する。
4. いずれの方法でも、選択範囲と交差する**実際の町丁目・字境界線**が地図上に描画され、含まれる世帯数(境界データの SETAI 属性、または国勢調査由来の世帯数)が自動集計される。
5. 「条件を選択」ボタンで属性条件を絞り込み、集計結果・ランク別色分けを確認。
6. 「画面をレポート画像として保存」または「印刷」機能で、現在の画面(パネル+地図)を画像/PDFとして出力。

範囲を広げすぎて対象市区町村が多くなりすぎた場合(目安12市区町村超)は、フリーズを防ぐため
読み込みを行わず「範囲が広すぎます」と表示する。その場合は地図を拡大するか、選択範囲を狭める。

### 3. 店舗管理機能(新規)

複数の店舗・拠点を登録し、グループ単位で管理できる。

- 店舗一覧: 店舗名、配布通数、半径、住所、緯度・経度、登録日時・更新日時を一覧表示。
- グループ情報: グループ名ごとに色(塗りつぶしRGB)・枠線幅・アイコン・ポイントサイズを設定でき、他ユーザーとの共有可否(共有フラグ)を切り替え可能。
- 店舗の追加はマウスで地図上をクリックして登録するほか、住所や既存店舗を参照して追加できる。
- 「半径」を指定して「描画」すると、選択した店舗を中心とした円商圏を地図上にまとめて表示。「反映」で登録済みの全店舗の商圏を一括描画する。
- ラベル表示のON/OFFが可能。

### 4. 配達プラン作成ウィザード(新規・中核機能)

チラシ・折込等の配布計画を、以下のステップに沿って作成できるウィザード形式のパネル。

1. **作成方法の指定**: 新規作成、または過去に作成した配達プランを検索して呼び出し複製する。
2. **商圏作成方法の指定**: 次のいずれかを選択する。
   - 市区町村商圏指定(都道府県→市区町村を複数選択)
   - 地図上の店舗を起点にした円商圏指定
   - 地図上の店舗を起点にした電車商圏指定
   - 地図上の店舗を起点にした自動車・徒歩商圏指定(所要時間ベース)
   - 任意商圏(自由描画のポリゴン)
   - 多店舗分析(複数店舗をまとめて処理)
   - 複数商圏を重ねる場合の「重なり率(%)」をしきい値として指定可能。
3. **新聞(配布媒体)の指定**: 対象とする新聞・配布媒体を一覧から選択し、「カバー率(◯%以下)」を条件として絞り込む(新聞折込を前提としたターゲティング機能)。
4. **指標の指定**: 「統計情報」カテゴリ(国勢調査、推計1歳刻み人口、推計年収別世帯数、推計家計消費、新購買志向、自動車カテゴリ・自動車メーカー車種・軽自動車メーカー等)から、集計したい指標をツリーから選び「選択された指標」欄に追加する。選択後は順序の入れ替えが可能。
5. **予算通数の指定**: 配布したい目標部数(予算通数)を入力すると、システムが配達見込世帯数をもとに対象エリアを自動選定する。配達見込世帯数がゼロの町丁目を除外するオプションや、統計上の世帯数に「掛け率(%)」を乗じて配達見込数を補正するオプションを持つ。あくまで目安であり、必ずこの通数を配達できることを保証するものではない旨を明示する。
6. ウィザード下部のアクション: 「地図選択」(範囲を地図上で再指定)、「分析開始」、「逆引き分析」(目標指標から逆算して条件を探索)、「明細」(内訳表示)、「保存」(プランの保存)、「レポート出力」、「キャンセル」。

### 5. 境界レイヤー・属性テーブル

- レイヤーは「都道府県界」「市区町村界」「町丁字界」「町丁字界(ポップアップ)」の4層構成とし、それぞれ表示/非表示、透過表示、ラベル表示切替、重ね順変更が可能。
- 町丁字界ポリゴンをクリックすると、その町丁目の名称をタイトルに持つポップアップを表示する(複数該当時は前後移動可能)。
- 各レイヤーは属性テーブルとして開くことができ、地図表示範囲によるフィルター、列の表示/非表示切り替え、全件のCSVエクスポートに対応する。

## 町丁目・字境界データ boundaries/

- 構成: boundaries/{都道府県コード2桁}/{市区町村コード5桁}.geojson + 都道府県ごとの index.json(市区町村一覧)。
- 各 Feature の主な properties:

| フィールド | 内容 |
|---|---|
| KEY_CODE | 町丁目・字を一意に識別するコード(属性データとの結合キー) |
| PREF / CITY | 都道府県コード / 市区町村コード |
| CITY_NAME | 市区町村名 |
| S_NAME | 町丁目・字名 |
| SETAI | 世帯数(基本の世帯数集計に使用) |
| AREA | 面積(平方km、任意) |
| LAT / LON | 代表点の緯度・経度(任意、ラベル配置やブックマークに利用) |
| ZIP | 郵便番号(任意) |

- 起動時に data/boundaries_index.json(各市区町村ファイルのbboxのみをまとめた軽量インデックス、約250KB)を読み込み、選択範囲のbboxと交差する市区町村ファイルだけを js/boundaryLoader.js が遅延読み込み・キャッシュする。
- boundaries/ の中身を差し替えた場合は、インデックスの再生成が必要:

```bash
python scripts/build_boundaries_index.py
```

## 属性データ(統計指標)について

世帯数・ジオメトリは boundaries/ から自動的に得られるが、その内訳の扱いを以下のように拡張する。

- 起動直後は従来どおり data/sample_stats.js のデモ用ダミーデータが読み込まれる。
- 「属性データ読み込み」から、実際の統計データ(国勢調査由来の性別・年齢別人口、世帯数など)をJSONファイルとして差し替え可能な点は従来仕様のまま維持する(ファイルはブラウザ内で処理され、外部送信されない)。
- 加えて、次のカテゴリを SEGMENT_CONFIG の拡張候補として追加する(いずれもデータが提供された場合のみ有効になる任意項目):
  - 推計年収別世帯数(一般世帯/持ち家世帯/借家世帯別の内訳)
  - 推計家計消費(品目別の推計消費額)
  - 新購買志向(消費者の購買傾向セグメント)
  - 自動車保有(自動車カテゴリ、メーカー・車種別、軽自動車メーカー別)
  - 居住期間(1年未満〜20年以上・不詳の年数区分)
  - 産業別就業者数(農林業、漁業、鉱業など主要産業分類別の就業者数)
- カテゴリ・選択肢のキー名は js/config.js の SEGMENT_CONFIG と一致させる。実データのカテゴリ構成が異なる場合は SEGMENT_CONFIG を編集すれば、UI・集計ロジックとも自動的に追従する。

### 期待するJSONスキーマ(拡張後)

```jsonc
[
  {
    \"key_code\": \"011010200\",
    \"population\": 320,
    \"segments\": {
      \"sexage\":   { \"male_20s\": 12, \"female_20s\": 10 },
      \"marriage\": { \"single\": 40, \"married\": 60 },
      \"family\":   { \"single_person\": 30, \"couple\": 20 },
      \"housing\":  { \"owned_house\": 40, \"rented_apartment\": 50 },
      \"income\":   { \"under300\": 20, \"300_500\": 30 },
      \"saving\":   { \"under100\": 25, \"100_500\": 40 },
      \"car_ownership\": { \"kei\": 15, \"compact\": 20 },
      \"residence_years\": { \"under5\": 30, \"5_10\": 20, \"over20\": 10 },
      \"consumption\": { \"food\": 12, \"housing_cost\": 8 }
    }
  }
]
```

- segments 以下のカテゴリは、対応するデータが無ければ省略可能(絞り込み条件として無視され、世帯数の絞り込みは行われない)。

### 属性条件による絞り込みの考え方

選択した属性条件に完全一致する世帯数を直接持っているわけではないため、各カテゴリ内の
「選択した選択肢の合計 ÷ カテゴリ全体の合計」を比率とし、複数カテゴリにまたがる場合はこれらの
比率が独立事象であるとみなして掛け合わせることで世帯数を推定する。真の交差集計データがある
場合は、segments の下に交差済みカテゴリ(例: sexage_x_income など)を追加し、
js/dataStore.js の estimateHouseholds を該当カテゴリ優先で参照するよう調整することで、より正確な集計に切り替えられる。

## 配達プラン・予算通数機能に関する補足

- 配達プランは保存・再読込ができ、過去の配達プランを複製して新しいプランのベースにできる。
- 予算通数(目標配布部数)に基づくエリア自動選定は、あくまで目安の提示であり、実際にその部数を配布できることを保証するものではない旨を画面上に明示する。
- 新聞(配布媒体)ごとのカバー率によるターゲティングは、新聞購読データなど外部から提供されるデータが存在する場合にのみ機能する任意機能とする。

## 地図まわりの補助機能

- ベースマップギャラリー: 地形図・衛星画像・道路地図の3種類を切り替え可能。
- オーバービューマップ: 現在の表示範囲を示す縮小地図をポップアップ表示。
- ブックマーク: よく使う表示範囲を名前を付けて保存し、ワンクリックで呼び出せる。
- 描画ツール: 商圏図形とは別に、点・線・フリーハンド線・矩形・円・楕円・フリーハンド図形・テキストラベルなどを地図上に自由に描画できる汎用アノテーション機能。
- 計測ツール: 距離・面積・座標(緯度経度)を計測できる。

## ファイル構成

```
index.html               エントリポイント
css/style.css             スタイル
js/config.js               セグメント条件・配色などの設定
js/dataStore.js            属性データ(KEY_CODEキー)の保持・集計ロジック
js/boundaryLoader.js       町丁目・字境界データの遅延読み込み・キャッシュ
js/map.js                  Leaflet地図・検索・商圏図形描画・境界ポリゴン表示
js/panel.js                 左サイドパネルのUI制御
js/report.js                html2canvasによるレポート画像出力
js/storeManager.js          店舗管理(店舗・グループの登録/編集/一括描画)【新規】
js/deliveryPlan.js          配達プラン作成ウィザード(商圏作成・新聞ターゲティング・指標選択・予算通数選定)【新規】
js/basemapGallery.js        ベースマップ切替【新規】
js/bookmarks.js             ブックマーク機能【新規】
js/main.js                  全体の結線・非同期読み込みフロー・集計
data/sample_stats.js        デモ用ダミー属性データ
data/boundaries_index.json  境界データの軽量bboxインデックス(build_boundaries_index.pyで生成)
scripts/build_boundaries_index.py  上記インデックスの生成スクリプト
boundaries/                 町丁目・字境界データ本体(都道府県×市区町村のGeoJSON)
```

## 使用ライブラリ (すべてCDN経由・無料)

| ライブラリ | 用途 |
|---|---|
| Leaflet | 地図表示 |
| OpenStreetMap タイル | ベースマップ(地形図・道路地図相当) |
| Esri World Imagery | 航空写真(衛星画像切替表示用) |
| Nominatim (OpenStreetMap) | 住所・地名検索(ジオコーディング) |
| Turf.js | 図形の交差判定・バッファ計算 |
| html2canvas | 画面キャプチャ(レポート出力) |
| OpenRouteService (任意) | 所要時間モードの道路網ベース等時間圏 |
| Font Awesome | アイコン |

## 元アプリとの主な違い

- ログイン機能・アカウント共有機能は実装していません(サーバーを持たないクライアント完結アプリのため)。店舗管理のグループ共有機能もローカル保存(ブラウザ内)に限定されます。
- レポートのメール送信機能は実装していません(バックエンドが必要なため)。画像/PDFダウンロードのみ対応。
- 「所要時間」「電車商圏」は、APIキー未設定時は近似円・簡易表示になります(道路網・鉄道網ベースの正確な到達圏を無償で得るには外部ルーティングAPIが必要です)。
- 「新聞(配布媒体)ターゲティング」「予算通数による自動エリア選定」「推計年収・家計消費・購買志向・自動車保有等の統計指標」は、対応する実データが提供された場合にのみ有効になる任意機能です。既定ではデモ用ダミーデータのみが利用可能です。
- 属性セグメントの絞り込みは独立仮定に基づく推定値です(交差集計データがある場合を除く)。
- fetch() を使用するため index.html を直接開く(file://)のではなく、ローカルサーバー経由で開く必要があります。
